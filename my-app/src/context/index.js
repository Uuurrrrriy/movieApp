import {createContext} from "react";

export const AppLinksContext = createContext({});

export const AppFooterIconsContext = createContext({});

export const AppSortingTypeContext = createContext({});
